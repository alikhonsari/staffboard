export function speedRackPlannerPlugin() {
  return {
    name: 'staffboard-speed-rack-planner',
    enforce: 'pre',
    transform(code, id) {
      if (!id.endsWith('/src/App.jsx')) return null
      let next = code

      if (!next.includes('speedRackPlanner: {')) {
        next = next.replace(
          `  rackLists: {`,
          `  speedRackPlanner: {\n    rackCount: '',\n    lead: '',\n    rackPrep: '',\n    infeed: '',\n    lineRecovery: '',\n    outfeed: '',\n    quality: '',\n    ob12: '',\n    support: '',\n    loa: '',\n    pto: '',\n    updatedAt: '',\n    updatedBy: '',\n  },\n  rackLists: {`
        )
      }

      if (!next.includes('const speedRackPlan =')) {
        const marker = '  return (\n    <div className={state.darkMode ? "app dark" : "app"}'
        const logic = `  const speedRackPlan = {\n    rackCount: '', lead: '', rackPrep: '', infeed: '', lineRecovery: '', outfeed: '', quality: '', ob12: '', support: '', loa: '', pto: '',\n    ...(dayState.speedRackPlanner || {}),\n  }\n  const speedRackRoleFields = [\n    ['lead', 'Lead'],\n    ['rackPrep', 'Rack Prep'],\n    ['infeed', 'Infeed'],\n    ['lineRecovery', 'Line Recovery'],\n    ['outfeed', 'Outfeed'],\n    ['quality', 'Quality'],\n    ['ob12', 'OB1 / OB2'],\n    ['support', 'Support'],\n  ]\n  const speedRackProductionHC = speedRackRoleFields.reduce((sum, [key]) => sum + numVal(speedRackPlan[key]), 0)\n  const speedRackCount = numVal(speedRackPlan.rackCount)\n  const speedRackHoursPerRack = speedRackCount > 0 ? (speedRackProductionHC * RACK_WEIGHT) / speedRackCount : 0\n  const speedRackTotalRoster = speedRackProductionHC + numVal(speedRackPlan.loa) + numVal(speedRackPlan.pto)\n  const speedRackActualProductionHC = typeof laborShareStats !== 'undefined' ? numVal(laborShareStats.speedProductionHeadcount) : activeProductionHeadcount\n  const speedRackHcGap = speedRackActualProductionHC - speedRackProductionHC\n  const speedRackPresets = [\n    { rackCount: 25, lead: 2, rackPrep: 4, infeed: 2, lineRecovery: 4, outfeed: 2, quality: 1, ob12: 2, support: 1 },\n    { rackCount: 20, lead: 2, rackPrep: 3, infeed: 2, lineRecovery: 3, outfeed: 2, quality: 1, ob12: 2, support: 1 },\n    { rackCount: 15, lead: 2, rackPrep: 2, infeed: 2, lineRecovery: 3, outfeed: 2, quality: 1, ob12: 2, support: 1 },\n  ]\n\n  const updateSpeedRackPlan = (patch) => {\n    if (!String(state.currentBoardId || '').startsWith('speed_')) return\n    updateDay((prev) => ({\n      ...prev,\n      speedRackPlanner: {\n        ...(prev.speedRackPlanner || {}),\n        ...patch,\n        updatedAt: nowString(),\n        updatedBy: user?.username || state.adminName || 'System',\n      },\n    }))\n  }\n\n  const applySpeedRackPreset = (preset) => {\n    const label = preset.rackCount + ' rack plan'\n    if (!confirm('Load the ' + label + '? LOA and PTO values will be preserved.')) return\n    updateSpeedRackPlan({ ...preset })\n  }\n\n  const clearSpeedRackPlan = () => {\n    if (!confirm('Clear the SPEED rack and labor planner for ' + state.selectedDay + '?')) return\n    updateSpeedRackPlan({ rackCount: '', lead: '', rackPrep: '', infeed: '', lineRecovery: '', outfeed: '', quality: '', ob12: '', support: '', loa: '', pto: '' })\n  }\n\n  const speedRackPlanText = () => [\n    (BOARD_PRESETS[state.currentBoardId]?.label || state.boardTitle) + ' — ' + state.selectedDay + ' — Week ' + String(weekInfo.week).padStart(2, '0'),\n    'Planned Racks: ' + (speedRackCount || 0),\n    'Planned Production HC: ' + speedRackProductionHC,\n    'Current SPEED Production HC: ' + speedRackActualProductionHC,\n    'HC Gap: ' + (speedRackHcGap > 0 ? '+' : '') + speedRackHcGap,\n    'Labor Hours per Rack: ' + speedRackHoursPerRack.toFixed(3),\n    'LOA: ' + numVal(speedRackPlan.loa) + ' | PTO: ' + numVal(speedRackPlan.pto),\n    'Total Roster Needed: ' + speedRackTotalRoster,\n    'Staffing Mix: ' + speedRackRoleFields.map(([key, label]) => label + ' ' + numVal(speedRackPlan[key])).join(' | '),\n    'Updated by: ' + (speedRackPlan.updatedBy || user?.username || state.adminName || 'System') + ' | ' + (speedRackPlan.updatedAt || new Date().toLocaleString()),\n  ].join(String.fromCharCode(10))\n\n  const copySpeedRackPlan = async () => {\n    const text = speedRackPlanText()\n    try { await navigator.clipboard.writeText(text); alert('SPEED rack plan copied.') }\n    catch { prompt('Copy this SPEED rack plan:', text) }\n  }\n\n`
        next = next.replace(marker, logic + marker)
      }

      if (!next.includes("setMainTab('speedPlanner')")) {
        const toolsButton = `          <button className={mainTab === 'tools' ? 'primary nav-tab active' : 'secondary nav-tab'} onClick={() => setMainTab('tools')}>Tools</button>`
        const plannerButton = `${toolsButton}\n          {String(state.currentBoardId || '').startsWith('speed_') ? <button className={mainTab === 'speedPlanner' ? 'primary nav-tab active' : 'secondary nav-tab'} onClick={() => setMainTab('speedPlanner')}>SPEED Planner</button> : null}`
        next = next.replace(toolsButton, plannerButton)
      }

      if (!next.includes('SPEED Rack & Labor Planner')) {
        const marker = `        ) : mainTab === 'comments' ? (`
        const panel = `        ) : mainTab === 'speedPlanner' ? (\n          String(state.currentBoardId || '').startsWith('speed_') ? (\n          <div className="board-shell speed-rack-planner-shell">\n            <div className="board-header">\n              <div><div className="title">SPEED Rack & Labor Planner</div><div className="small">Planning calculator based on the uploaded rack model. It does not change assignments or live TPH.</div></div>\n              <div><span className="pill">{BOARD_PRESETS[state.currentBoardId]?.label}</span><span className="pill">{state.selectedDay}</span><span className="pill">Week {String(weekInfo.week).padStart(2, '0')}</span></div>\n            </div>\n\n            <div className="speed-rack-kpi-grid">\n              <div className="summary-card kpi-highlight-card"><div className="summary-label">Planned Racks</div><div className="summary-value">{speedRackCount || 0}</div></div>\n              <div className="summary-card kpi-highlight-card"><div className="summary-label">Planned Production HC</div><div className="summary-value">{speedRackProductionHC}</div></div>\n              <div className="summary-card kpi-highlight-card"><div className="summary-label">Current Production HC</div><div className="summary-value">{speedRackActualProductionHC}</div></div>\n              <div className="summary-card kpi-highlight-card"><div className="summary-label">HC Gap</div><div className={\`summary-value ${'${speedRackHcGap < 0 ? \'negative\' : speedRackHcGap > 0 ? \'positive\' : \'\'}'}\`}>{speedRackHcGap > 0 ? '+' : ''}{speedRackHcGap}</div></div>\n              <div className="summary-card kpi-highlight-card"><div className="summary-label">Labor Hours / Rack</div><div className="summary-value">{speedRackHoursPerRack.toFixed(3)}</div></div>\n              <div className="summary-card kpi-highlight-card"><div className="summary-label">Total Roster Needed</div><div className="summary-value">{speedRackTotalRoster}</div></div>\n            </div>\n\n            <div className="summary-card-block card">\n              <div className="table-title-row"><div><div className="table-kicker">Quick Plans from the Workbook</div><div className="small">Load a staffing mix, then adjust any role, LOA, or PTO value.</div></div></div>\n              <div className="speed-rack-preset-row">{speedRackPresets.map((preset) => { const hc = Object.entries(preset).filter(([key]) => key !== 'rackCount').reduce((sum, [, value]) => sum + Number(value || 0), 0); const hpr = (hc * RACK_WEIGHT) / preset.rackCount; return <button className="secondary" key={preset.rackCount} onClick={() => applySpeedRackPreset(preset)}><strong>{preset.rackCount} Racks</strong><span>{hc} HC · {hpr.toFixed(3)} hrs/rack</span></button> })}</div>\n            </div>\n\n            <div className="summary-card-block card">\n              <div className="table-kicker">Rack Target and Staffing Mix</div>\n              <div className="speed-rack-input-grid">\n                <label className="speed-rack-input emphasis"><span>Planned Rack Count</span><input type="number" min="0" step="1" value={speedRackPlan.rackCount} onChange={(e) => updateSpeedRackPlan({ rackCount: e.target.value })} /></label>\n                {speedRackRoleFields.map(([key, label]) => <label className="speed-rack-input" key={key}><span>{label}</span><input type="number" min="0" step="1" value={speedRackPlan[key]} onChange={(e) => updateSpeedRackPlan({ [key]: e.target.value })} /></label>)}\n              </div>\n              <div className="speed-rack-formula">\n                <strong>Calculation:</strong> ({speedRackProductionHC} Production HC × {RACK_WEIGHT}) ÷ {speedRackCount || 0} Racks = <strong>{speedRackHoursPerRack.toFixed(3)} labor hours per rack</strong>\n              </div>\n            </div>\n\n            <div className="two-col-layout">\n              <div className="summary-card-block card">\n                <div className="table-kicker">Roster Allowance</div>\n                <div className="row two"><label>LOA<input type="number" min="0" step="1" value={speedRackPlan.loa} onChange={(e) => updateSpeedRackPlan({ loa: e.target.value })} /></label><label>PTO<input type="number" min="0" step="1" value={speedRackPlan.pto} onChange={(e) => updateSpeedRackPlan({ pto: e.target.value })} /></label></div>\n                <div className="speed-rack-roster-equation">{speedRackProductionHC} Production HC + {numVal(speedRackPlan.loa)} LOA + {numVal(speedRackPlan.pto)} PTO = <strong>{speedRackTotalRoster} Total Roster Needed</strong></div>\n              </div>\n              <div className="summary-card-block card">\n                <div className="table-kicker">Planning Notes</div>\n                <div className="small">Current live Production HC comes from active SPEED production assignments. This calculator is separate so planning changes cannot accidentally alter the staffing board.</div>\n                <div className="row two"><button className="secondary" onClick={copySpeedRackPlan}>Copy Plan Summary</button><button className="danger" onClick={clearSpeedRackPlan}>Clear Planner</button></div>\n                <div className="tiny">Last updated: {speedRackPlan.updatedAt || 'Not yet saved'} · {speedRackPlan.updatedBy || '—'}</div>\n              </div>\n            </div>\n          </div>\n          ) : <div className="board-shell"><div className="summary-card-block card"><div className="title">SPEED Planner</div><div className="small">This calculator is available only on SPEED Day and SPEED Night boards.</div></div></div>\n`
        next = next.replace(marker, panel + marker)
      }

      return next === code ? null : { code: next, map: null }
    },
  }
}
