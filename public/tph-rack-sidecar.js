(() => {
  // Disabled: this layout helper was causing intermittent insertBefore crashes during React re-render.
  // Keeping this file as a no-op prevents cached index.html versions from breaking the app.
})()
